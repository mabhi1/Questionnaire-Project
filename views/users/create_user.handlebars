<div>
    <h1 class="user-login">User Signup</h1>
    <form id="signup-form" class="form" action="/users" method="post" enctype="multipart/form-data">
    <div class="form-group">
        <label for="firstName">First Name(Only letters) </label>
        <input type="text" id="firstName" class="form-control" name="firstName" placeholder="Enter your first name" value="{{body.firstName}}" required>
        </div>
        <div class="form-group">
        <label for="lastName">Last Name(Only letters) </label>
        <input type="text" id="lastName" class="form-control" name="lastName" placeholder="Enter your last name" value="{{body.lastName}}" required>
        </div>
        <div class="form-group">
        <label for="password">Password (Please enter at least 6 characters. Spaces in the middle are not allowed.)</label>
        <input id="password" name="password" class="form-control" type="password" placeholder="Enter your desired password" required>
        </div>
        <div class="form-group">
        <label for="emailAddress">Email Address</label>
        <input type="text" id="emailAddress" class="form-control"  name="emailAddress" placeholder="Enter your email address" value="{{body.emailAddress}}" required>
        </div>
        <div class="form-group">
        <label for="displayName">Display Name (Some things in life are permanent - this is one of those! Also, we'll let you know if the name is not unique)</label>
        <input type="text" id="displayName" class="form-control" name="displayName" placeholder="Enter your desired displayName" value="{{body.displayName}}" required>
        </div>
        <div class="form-group">
        <label for="profileImage">Profile Image: (We will allocate a default avatar, if you choose not to upload)
            <br>Accepted formats: jpg, jpeg, bmp, gif, png
        </label>
            <div class="custom-file">
                <input type="file" id="profileImage" class="form-control" name="profileImage">
            </div>
        </div>    
        <br>
        <button type="submit" class="btn btn-primary float-right">Create Account</button>
    </form>

    {{#if error}}
    <div class="alert alert-warning d-inline-block" role="alert">
        {{this.error}}
    </div>
    {{/if}}
    <br>
    <a href="/site/login">Already have an account? Click here to log-in</a>
</div>